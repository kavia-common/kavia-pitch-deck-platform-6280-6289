# Example environment variables used by the Slidev frontend.
# Do NOT commit real secrets. The CI/Orchestrator will populate .env.

# Public base path when deploying under a subpath (optional):
VITE_PUBLIC_BASE=/

# Site URL used by integrations (if any in future). Example:
# VITE_SITE_URL=https://example.com
